apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatbot-chart.fullname" . }}
  labels:
    {{- include "chatbot-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chatbot-chart.selectorLabels" . | nindent 4 }}
